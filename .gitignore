# ---- Android / Gradle ----
*.iml
.gradle/
build/
captures/
local.properties
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class
release/

# ---- Android Studio / IntelliJ ----
.idea/
*.hprof

# ---- Python (PC 侧 Agent) ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- 密钥与环境变量 ----
.env
.env.*
!.env.example
*.keystore
*.jks

# ---- 实验产物 ----
# 节点树 dump、logcat、截图等原始数据体积大且噪声多，
# 只把结论和有代表性的片段写进 EXPERIMENTS.md，原始文件不入库
dumps/
logs/
*.log
window_dump.xml

# loop 的 trajectory 落盘目录。默认不入库（每跑一次就多一批）；
# 要留作答辩材料的那几次，单独拷进 docs/ 再提交。
runs/

# 演示素材（体积大，走外链）
*.mp4
# 例外：交付用的演示视频（8.3MB，远低于 GitHub 的 50MB 告警线，
# 刻意不用 LFS —— 评审方 clone 时不该被要求额外装东西）
!docs/media/demo.mp4
*.mov

# 截图例外：EXPERIMENTS.md / README 引用的图放 docs/ 下，需要入库
!docs/**/*.png
!docs/**/*.gif

# ---- OS ----
.DS_Store
Thumbs.db
desktop.ini
